{
  "name": "wwwalidator",
  "version": "1.0.0",
  "description": "Simple, but big validator for one-time-all-checking",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "validator",
    "validate",
    "checker"
  ],
  "author": "Patryk Poźniak",
  "license": "ISC",
  "devDependencies": {
    "mitm": "^1.3.2"
  },
  "dependencies": {
    "request": "^2.81.0",
    "request-promise": "^4.2.0"
  }
}
